.modal__top
  .modal__info
    = link_to issue_home_path(@opinion.issue) do
      = image_tag @opinion.issue.logo.xs.url, class: 'circle-30 img-circle'
      %span.text-primary= @opinion.issue.title
  %button.modal__btn(type="button" class="close" data-dismiss="modal" aria-label="Close")
    = image_tag 'icon_close.png'

.modal__content
  .text-center
    = render 'body'
  = render 'posts/post_header', post: @opinion.acting_as, only_buttons: true

.modal__comment
  = render 'comments/comments', postable: @opinion, style: :opinion
