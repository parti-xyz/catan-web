.editors-panel.hidden-xs
  .media
    .media-body
      %h4
        위키에 정리해 보세요!
        %small
          공유하고 싶은 정보를 정리해 보세요.
          %a.btn.btn-default.btn-xs{href: "https://opentutorials.org/module/767/6133", target: '_blank'} 위키란?
    .media-right
      - if user_signed_in?
        - if @issue.postable?(current_user)
          = link_to [:edit, issue.wiki], class: 'btn btn-primary btn-shadow editor-btn' do
            %i.fa.fa-pencil
            = t('labels.edit_wiki_button')
        - else
          = render 'issues/join_button'
      - else
        = link_to '#', class: 'btn btn-primary btn-shadow editor-btn', data: { toggle: 'parti-login-overlay' } do
          %i.fa.fa-pencil
          = t('labels.edit_wiki_button')
