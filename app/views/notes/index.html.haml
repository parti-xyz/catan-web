= render 'top_banner'
%section#notes
  .container
    %h3.page-title
      담벼락

    .row.masonry-container{data: {last_id: @notes.last.try(:id), is_last: @is_last_page.to_s}}
      - if @notes.empty?
        .col-xs-12
          .bg-default
            %i.fa.fa-info-circle
            해당되는 글이 없습니다.
      - else
        = render 'issues/slug_notes_page', show_issue: true
    .page_waypoint.text-center{'data-target': '.row.masonry-container', 'data-url': notes_path(format: :js)}
      .page_waypoint__loading
        %i.fa.fa-3x.fa-spinner.fa-pulse

