.posts
  .row.masonry-container{data: {last_id: @notes.last.try(:id), is_last: @is_last_page.to_s}}
    - if user_signed_in?
      .col-sm-4.card
        = render 'notes/new_form', issue: @issue

    - postables.each do |postable|
      = render postable, show_issue: local_assigns[:show_issue]
