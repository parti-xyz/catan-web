var $chevron_buttons = $('<%= j render('users/chevron_buttons', group: current_group, user: @user) %>');
$("ul[aria-labelledby='<%= user_chevron_dom_id(@user) %>']").html($chevron_buttons);
parti_partial$($chevron_buttons);
