$('.row.masonry-container').data('last-id', '<%= @posts.last.try(:id) %>');
$('.row.masonry-container').data('is-last', <%= @is_last_page %>);
var $posts = parti_origin_partial("<%= escape_javascript(render 'polls/slug_polls_page', show_issue: true) %>");
$('.row.masonry-container').append($posts).masonry('appended', $posts);

