$('.row.masonry-container').data('last-id', '<%= @notes.last.try(:id) %>');
$('.row.masonry-container').data('is-last', <%= @is_last_page %>);
var $posts = parti_origin_partial("<%= escape_javascript(render 'notes/slug_notes_page') %>");
$('.row.masonry-container').append($posts).masonry('appended', $posts);

