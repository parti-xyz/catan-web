$('.row.masonry-container').data('last-id', '<%= @opinions.last.try(:id) %>');
$('.row.masonry-container').data('is-last', <%= @is_last_page %>);
var $posts = parti_origin_partial("<%= escape_javascript(render 'issues/slug_opinions_page') %>");
$('.row.masonry-container').append($posts).masonry('appended', $posts);

