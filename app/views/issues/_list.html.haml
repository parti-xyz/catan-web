.row
  - if params[:parti_order].present?
    - if params[:parti_order] == 'hottest'
      - issues = @issues.hottest
    - else
      - issues = @issues.order(params[:parti_order])
    - issues.each do |issue|
      .col-sm-4.col-xs-6
        .issue-card-wrapper
          = render 'issues/card', issue: issue
  - else
    - issues = @issues.order(:title)
    - issues.each do |issue|
      .col-sm-4.col-xs-6
        .issue-card-wrapper
          = render 'issues/card', issue: issue
