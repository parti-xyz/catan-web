= render 'issues/issue_header', active_tab: :wikis

%section#wikis
  .container
    - if can? :update, @issue.wiki
      .editors-panel
        .media
          .media-body.hidden-xs
            %h3
              위키에 정리해 보세요!
              %small 공유하고 싶은 정보를 정리해 보세요.
              %a.btn.btn-default 위키란?
          .media-right
            = link_to [:edit, @issue.wiki], class: 'btn btn-primary btn-lg btn-shadow editor-btn' do
              %i.icon-wiki
              = t('labels.edit_wiki_button')
    .body
      = raw @issue.wiki.body

/ = form_for @issue.wiki do |f|
/   .form-group
/     = f.label :body
/     = f.text_area :body, class: 'form-control redactor', rows: 40, cols: 120
/     = f.hidden_field :issue_id, class: 'form-control'
/   .form-group
/     = f.submit '저장', class: 'btn btn-primary'
