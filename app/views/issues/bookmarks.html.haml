%section.default-page
  .container
    .row
      .col-xs-12.col-sm-8
        %h4.posts-header__title.vertical-shrinking-top
          = (@group.indie? ? "개인 빠띠" : @group.title_basic_format)
          %span.text-nowrap 메뉴 설정
          %small
            = group_my_parties_section_title(@group)
            메뉴의 순서를 정합니다
        .panel.panel-default
          .panel-body
            %ul.list-unstyled
              - @issues.each do |issue|
                - bookmark_control = capture do
                  = render 'issues/bookmark_button', issue: issue
                = render 'issues/issue_line', issue: issue, right_block: bookmark_control
      - unless is_small_screen?
        = render 'application/aside'
