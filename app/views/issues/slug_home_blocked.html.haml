= render 'issues/issue_header', active_tab: :home, blocked: true

%section#polls.polls--issue
  .container
    .editors-panel
      .media
        .media-body
          %h4
            비밀빠띠입니다.
          - if @issue.member_requested? current_user
            %p
              이 빠띠를 보시려면 먼저 가입승인을 받아야합니다.
              %span.text-nowrap
                %strong<> 빠띠메이커의 가입승인
                을 기다려주세요.
          - else
            %p
              비밀빠띠를 보시려면 먼저 가입신청을 해주세요.
            %div(class="#{dom_id(@issue)}-member" style="    padding-bottom: 12px;")
              - if user_signed_in?
                = link_to smart_members_or_member_requests_parti_path(@issue), method: :post, class: 'btn btn-md btn-primary' do
                  가입하기
              - else
                = link_to '가입하기', '#', data: { toggle: 'parti-login-overlay', label: '가입', 'after-login': h({action: :issue_member, id: @issue.id}.to_json) }, class: 'btn btn-md btn-primary'
