%section#facebook-like
  .container
    .fb-like{"data-action": "like", "data-href": "https://www.facebook.com/PartiUnion/", "data-layout": "standard", "data-share": "false", "data-show-faces": "false"}
%section#home
  .container
    .row
      .col-xs-12.col-sm-8
        %section#parties-all.margin-bottom-default
          .parties-all
            .parties-filter
              .parties-search-form
                = form_tag issues_path, method: :get, id: 'js-search-parties-form' do
                  = hidden_field_tag :sort
                  .input-group.input-group-lg.input-group-keyword
                    %input.form-control{name: "keyword", placeholder: "무엇에 관심있으세요?", value: params[:keyword], type: "search", 'data-action': 'parti-clearable-search'}
                    %span.input-group-btn
                      %button.btn.btn-group-default
                        %i.fa.fa-search
            - if params[:keyword].present?
              - if @issues.any?
                .parties-all-list
                  = render 'issues/page', issues: @issues, has_aside: true
              - else
                .bg-default
                  %i.fa.fa-info-circle
                  검색 결과가 없습니다.
        - if params[:keyword].blank?
          = render Group.all[0..4]
          .panel.panel-primary.panel-group-info
            .panel-body
              %p
                여러분의 모임&middot;조직만을 위한 그룹을 만들어 보세요. 그룹 안에서 여러 빠띠를 개설할 수 있습니다. 그룹은 공개형과 비공개형으로 나뉩니다.
              %p
                = link_to '그룹 개설을 신청합니다', 'https://goo.gl/forms/ldIKoqbRkUefqpgU2', class: 'btn btn-primary', target: '_blank'
                = link_to '가격 정책 안내', '/service', style:"text-decoration:underline;margin-left:10px;", target: '_blank'
          = render Group.all[5..-1]
      - unless is_small_screen?
        = render 'application/aside'
