%section#issues
  .container
    .page-header
      %h2
        빠띠 찾기
        %small (#{Issue.count})
    .row
      - @issues.each do |issue|
        .col-xs-12.col-sm-4
          = render 'issues/card', issue: issue
