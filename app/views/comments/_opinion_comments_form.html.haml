%a.anchor#comment-form
.comment-form.clearfix
  = form_for [postable.acting_as, Comment.new], remote: true, class: 'comment-form' do |f|
    = hidden_field_tag :container, "##{dom_id(postable)}-comments ##{postable.vote_by(current_user).choice} ul.media-list"
    = hidden_field_tag :count_label, "##{dom_id(postable)}-comments .comments--count.comments--count__#{postable.vote_by(current_user).choice}"
    = hidden_field_tag :body_control, "##{dom_id(postable)}-comments .comment-form textarea#comment_body"
    .thumb.circle-50.img-circle.comment-form__img{style: "background-image: url(#{current_user.image.sm.url});"}
    .comment-form__area
      = image_tag 'icon_tooltip_left2.png'
      = f.text_area :body, class: 'form-control'
    = f.submit '게시', class: 'btn btn-primary comment-form__btn'
