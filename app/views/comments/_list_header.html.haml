- if post.latest_comments.any?
  - if post.followed_comments_count > 2 or local_assigns[:in_modal]
    .more{data: { toggle: "parti-post-modal", target: '#post-modal', url: polymorphic_url(post.specific) }}
      %b>
        %i.fa.fa-comment-o
        총
        %span.post-card-with-comments__count #{post.followed_comments_count}
        개
      %span.hidden-xs 의 수다
      - unless local_assigns[:in_modal]
        \|
        %span.text-primary 이전 수다 더보기...
- if post.followed_comments_count == 0 and local_assigns[:in_modal]
  .more{data: { toggle: "parti-post-modal", target: '#post-modal', url: polymorphic_url(post.specific) }}
    %b>
      %i.fa.fa-comment-o
      첫 수다
    %span 를 남겨주세요.
