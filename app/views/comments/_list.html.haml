.comments.bg-line
  - if comments.empty?
    .bg-default
      %i.fa.fa-info-circle
      아직 글이 없습니다.
  - else
    = render 'comments/comments_per_page', comments: comments, show_issue: local_assigns[:show_issue]

