var $comment_in_list = $.parse$("<%= escape_javascript(render 'comment_in_list', comment: @comment) %>");
var $comment_in_post = $.parse$("<%= escape_javascript(render 'comment', comment: @comment) %>");
parti_prepare_post_modal($comment_in_list);
parti_prepare($comment_in_list);
parti_prepare_post_modal($comment_in_post);
parti_prepare($comment_in_post);

$('.<%= dom_id(@comment) %>[data-parti-comment-style=in-list]').replaceWith($comment_in_list);
$('.<%= dom_id(@comment) %>[data-parti-comment-style=in-post]').replaceWith($comment_in_post);
