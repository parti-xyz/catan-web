var specific_partial = "<%= escape_javascript(render 'roll_calls/invite_form_modal', event: @roll_call.event) %>";
$('.js-invite-form-modal').replaceWith(specific_partial);
parti_partial$($('.js-invite-form-modal'), true);

<%= render 'events/replace_card', post: @roll_call.event.post %>

<% unless @roll_call.status.invite? %>
  UnobtrusiveFlash.showFlashMessage("<%= t('errors.messages.events.already_responed_invitation.fail_to_destroy') %>");
<% end %>
