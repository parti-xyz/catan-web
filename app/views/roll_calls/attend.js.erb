<% if @roll_call.try(:attend?) %>
  <%= render 'events/replace_card', post: @event.post %>
<% else %>
  UnobtrusiveFlash.showFlashMessage("<%= t('errors.messages.unknown') %>", {type: 'error'});
<% end %>
