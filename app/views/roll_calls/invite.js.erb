var specific_partial = "<%= escape_javascript(render 'roll_calls/invite_form_modal', event: @event, invitee: @invitee) %>";
$('.js-invite-form-modal').replaceWith(specific_partial);
parti_partial$($('.js-invite-form-modal'), true);

$submit_button = $('.js-invite-form-modal input[type="submit"]');
$.rails.enableElement($submit_button);
$submit_button.removeAttr('disabled');

<%= render 'events/replace_card', post: @event.post %>
