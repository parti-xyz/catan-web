$('.post-container').data('last-stroked-at', '<%= @posts.last.try(:last_stroked_at).try(:to_i) %>');
$('.post-container').data('is-last', <%= @is_last_page %>);
var $posts = $("<%= j(render 'posts/page', posts: @posts, show_issue: true) %>");

$('.post-container').append($posts)
parti_partial$($posts);
