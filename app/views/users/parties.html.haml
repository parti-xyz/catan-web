= render 'user_header'
%section#user-gallery
  .container
    .row
      .col-sm-8.col-xs-12
        %section#content-tab-section
          = render 'users/content_tabs', active_tab: :parties, counts_container: @user.writing_counts
        .parties
          = render 'issues/list'

      = render 'aside'
