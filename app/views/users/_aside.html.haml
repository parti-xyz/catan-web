.col-sm-4.hidden-xs
  #aside
    .aside_issue

      - if host_group.comprehensive_joined_by?(@user)
        %h5.aside-title= host_group.aside_title
      - if @user.member_issues.only_group(host_group).any?
        %ul.list-unstyled.aside-issues
          = render customize_partial_path(@user.member_issues.only_group(host_group).reject { |issue| issue.private_blocked?(current_user) }, 'issue_line')

      - Group.comprehensive_joined_by(@user).but(host_group).each do |group|
        - next if group.private_blocked? current_user
        %h5.aside-title
          = link_to group.aside_title, smart_group_url(group)
        %ul.list-unstyled.aside-issues
          = render customize_partial_path(@user.member_issues.only_group(group).reject { |issue| issue.private_blocked?(current_user) }, 'issue_line')

    = render 'application/footer'
