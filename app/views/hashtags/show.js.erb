<% if @posts.empty? %>
if($('#posts .posts').data('last-stroked-at') === '') {
  $('#no-data').show();
}
<% end %>

$('#posts .posts').data('last-stroked-at', '<%= @posts.last.try(:last_stroked_at).try(:to_i) %>');
$('#posts .posts').data('is-last', <%= @is_last_page %>);
var $posts_doms = $("<%= j(render 'posts/page', posts: @posts, show_issue: true) %>");

$('#posts .posts').append($posts_doms);
parti_partial$($posts_doms);
