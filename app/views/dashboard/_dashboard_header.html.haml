%section#dashboard-header-section
  .container
    .jumbotron.jumbotron-transparent
      %h4
        내 빠띠
      %p
        %i.fa.fa-at>
          = current_user.nickname
          님이 구독 중인 빠띠의 #{Article.model_name.human}, #{Talk.model_name.human}, #{Opinion.model_name.human}를 모았습니다.

      .issues
        - current_user.watched_issues.each do |issue|
          = link_to issue_home_path(issue), class: 'btn btn-default btn-sm' do
            = image_tag issue.logo.sm.url
            = issue.title

