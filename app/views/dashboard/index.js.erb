$('#posts .posts').data('last-id', '<%= @posts.last.try(:id) %>');
$('#posts .posts').data('is-last', <%= @is_last_page %>);
var $posts_doms = $("<%= escape_javascript(render 'posts/page', show_issue: true) %>");
$('#posts .posts').append($posts_doms);
parti_partial$($posts_doms);

