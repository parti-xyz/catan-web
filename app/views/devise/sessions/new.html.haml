.bs-component
  %section#sign-up
    %h4.sign-up__title 로그인
    %h1.text-center.sign-up__txt
      수다로 정치하자,
      %br
      빠띠에서 파티하자
    .sign-up__form
      = link_to omniauth_authorize_path(resource_name, :facebook), class: 'btn-sns btn-block' do
        = image_tag 'icon_fb_color.png'
        페이스북 아이디로 빠띠하기
      = link_to omniauth_authorize_path(resource_name, :twitter), class: 'btn-sns btn-block' do
        = image_tag 'icon_tw_color.png'
        트위터 아이디로 빠띠하기
      = link_to omniauth_authorize_path(resource_name, :google_oauth2), class: 'btn-sns btn-block' do
        = image_tag 'icon_gp_color.png'
        구글플러스 아이디로 빠띠하기

      .devider
        %span 또는

      = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
        = f.hidden_field :provider, value: 'email'
        .form-group
          = f.email_field :email, placeholder: '이메일을 입력하세요', autofocus: true, class: 'form-control'
        .form-group
          = f.password_field :password, placeholder: '비밀번호를 입력하세요', autocomplete: "off", class: 'form-control'
        .form-group
          - if devise_mapping.rememberable?
            = f.check_box :remember_me
            = f.label :remember_me, class: 'remember_me'
        .form-group
          = f.submit '로그인', class: 'btn btn-primary btn-block'


      = render "devise/shared/links"
