.bs-component
  %section#sign-up
    %h4.sign-up__title 아이디
    %h2.text-center.sign-up__txt
      빠띠에서 당신의 이름이 될
      %br
      멋진 아이디를 만들어주세요.
    .sign-up__form
      = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
        = devise_error_messages!
        = f.hidden_field :provider
        = f.hidden_field :remember_me
        .form-group
          = f.label :nickname do
            빠띠에서 사용할 아이디를 넣어 주세요.
          = f.text_field :nickname, autofocus: true, class: 'form-control'
          .text-primary
            * 한글, 알파벳, 숫자, _ 만 가능합니다.
        .form-group
          = f.submit '계속하기', class: 'btn btn-primary btn-block'

        = form_tag nil, method: :get do
          = hidden_field_tag(:after_login)
          %label.remember_me
            "계속하기"를 클릭하면
            %a.text-primary{href: '/terms', target: '_blank'}
              %b 이용약관
            ,
            %a.text-primary{href: '/privacy', target: '_blank'}
              %b 개인정보처리
            방침 내용을 이해하고
            동의하게 됩니다.
