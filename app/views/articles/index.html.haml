= render 'top_banner'
%section#articles
  .container
    %h3.page-title
      자료
      %small 모든 빠띠의 최근 자료입니다.
    .row.masonry-container{data: {last_id: @articles.last.try(:id), is_last: @is_last_page.to_s}}
      - if @articles.empty?
        .bg-default
          %i.fa.fa-info-circle
          해당되는 글이 없습니다.
      - else
        = render 'issues/slug_articles_page', show_issue: true
    .page_waypoint.text-center{'data-target': '.row.masonry-container', 'data-url': articles_path(format: :js)}
      .page_waypoint__loading
        %i.fa.fa-3x.fa-spinner.fa-pulse

