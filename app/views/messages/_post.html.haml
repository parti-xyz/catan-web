%div{id: "#{dom_id(message)}-url-source", data: { url: smart_post_url(post) } }
  - if message.action.to_s == 'pinned'
    %strong
      %span
        #{message.sender.nickname}님이 게시글을
      %span
        공지했습니다
  - else
    %strong
      %span
        #{post.user.nickname}님이 게시글에서
      %span
        나를 언급했습니다
%br
= link_to excerpt(post.specific_desc, length: 100), post
