.col-sm-4.hidden-xs
  #aside
    .aside_user
      = render 'users/user_panel', user: current_user
    - if user_signed_in?
      .aside_issue
        .aside_issue_btn
          = link_to '빠띠만들기', parties_new_intro_path, class: 'btn btn-primary btn-block'

        - if host_group.nested_joined_by?(current_user)
          %h5.aside-title= host_group.aside_title
        - if current_user.member_issues.only_group(host_group).any?
          %ul.list-unstyled.aside-issues
            - current_user.member_issues.only_group(host_group).each do |issue|
              = render '/issues/issue_line', issue_line: issue

        - Group.nested_joined_by(current_user).but(host_group).each do |group|
          %h5.aside-title
            = link_to group.aside_title, smart_group_url(group)
          %ul.list-unstyled.aside-issues
            - current_user.member_issues.only_group(group).each do |issue|
              = render '/issues/issue_line', issue_line: issue

    = render 'application/footer'
