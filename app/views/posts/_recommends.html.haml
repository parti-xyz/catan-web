%section{id: "#{dom_id(issue)}-recents", style: "background-color: #fafafa; padding: 50px 0;" }
  .container
    %h4{ style: 'font-weight: 200' }
      %strong
        = link_to issue_path(issue) do
          %i.fa.fa-hashtag>
          = issue.title
      빠띠의 다른 글도 읽어보세요.
    %div
      - issue.posts.recommends(local_assigns[:exclude]).each do |post|
        = render post.specific
    %div{ style: 'margin: 20px 0;' }
      = link_to issue_home_path(issue), class: 'btn btn-primary btn-block' do
        %i.fa.fa-hashtag>
        #{issue.title} 빠띠의 모든 글 보기 (#{issue.posts.count})
        %i.fa.fa-arrow-right

%section{id: "#{dom_id(issue)}-commiters", style: "padding: 50px 0;" }
  .container
    %h4{ style: 'font-weight: 200' }
      %strong
        = link_to issue_path(issue) do
          %i.fa.fa-hashtag>
          = issue.title
      빠띠는
      %strong= issue.watches.count
      명의 구독하고 있습니다.

    %div{ style: 'padding-top: 10px' }
      .row
        - issue.watched_users.each do |user|
          .col-xs-12.col-sm-4
            = render user
        .col-xs-12.col-sm-12
          .panel.panel-default
            .panel-body.text-center
              .text-muted{ style: 'margin: 0' }
                %strong
                  = link_to issue_path(issue) do
                    %i.fa.fa-hashtag>
                    = issue.title
                빠띠를 #{"구독하고" if !user_signed_in? or !issue.watched_by?(current_user)} 함께 만들어주세요
              %div{ style: 'margin-top: 10px' }
                - if !user_signed_in? or !issue.watched_by?(current_user)
                  - if user_signed_in?
                    = render 'watches/watch', issue: issue
                  - else
                    = link_to omniauth_authorize_path(User, :facebook), class: 'btn btn-info', style: 'margin-top: 0.5em; max-width: 100%; word-wrap: break-word; white-space: pre-line; text-align:center;' do
                      %i.fa.fa-facebook
                      페이스북아이디로 빠띠에 로그인
                - else
                  = link_to issue_home_path(issue) do
                    글쓰기
                    %i.fa.fa-arrow-right
