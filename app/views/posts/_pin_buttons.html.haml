- if post.pinned?
  = link_to unpin_post_path(post), method: :delete, remote: true, data: { disable_with: '...' } do
    고정 해제하기
- else
  = link_to pin_post_path(post), method: :post, remote: true, data: { disable_with: '...' } do
    고정하기
