- if post.pinned?
  = link_to [:unpin, post], method: :delete, remote: true, class: post_pin_buttons_dom_class(post) do
    공지내리기
- else
  = link_to [:pin, post], method: :post, remote: true, class: post_pin_buttons_dom_class(post) do
    공지걸기
