.notifications{ data: { toggle: 'dropdown', target: 'message-dropdown.toggle' } }
  %i.fa.fa-bell
  - if @important_messages_count > 0
    %i.fa.fa-circle.circle{ data: { target: 'message-dropdown.circle', 'last-message-id': @messages&.first&.id } }
.dropdown-menu.dropdown-menu-right.messages{ data: { target: 'message-dropdown.menu' } }
  .dropdown-header.front-navbar-message-header 알림
  - if @messages.any?
    - @messages.each do |message|
      .dropdown-item.front-navbar-message-item{ class: ('unread' if message.unread?) }
        = render message
  - else
    .dropdown-item
      %i.fa.fa-info-circle
      아직 알림이 없습니다.
