%section.text-center
  .container
    - if current_group.member_requested?(current_user)
      %h3.text-keepall
        %i.fa.fa-cube
        이제 곧 초대드릴게요!
      .simple-box.text-center{ style: 'padding: 2rem 0'}
        %p.lead
          %p
            %strong #{current_user.nickname}
            님의 가입 신청을 운영진이 검토 중입니다.
            %br
            곧 처리될 예정이니 조금만 기다려 주세요.
          %p
            감사합니다.

    - else
      .simple-box.text-center{ style: 'padding: 2rem 0'}
        %h3
          %i.fa.fa-cube
          안녕하세요,
          %br
          #{current_group.title}에
          %span.text-nowrap 오신 것을
          %span.text-nowrap 환영합니다!
        - if user_signed_in?
          %p
            이 그룹에서 활동하려면
            %br
            가입이 필요합니다.
          %p
            지금 가입 신청 해주세요.
          %p.pt-3
            = link_to new_front_member_request_path, class: 'btn btn-md btn-primary btn-block' do
              가입하기
        - else
          %p
            계정을 만들고 그룹에 가입한 뒤에
            %br
            이 그룹에서 활동할 수 있습니다.
          %p
            먼저 계정을 만들어 주세요.
          %p.pt-3
            = link_to pre_sign_up_front_users_path, class: 'btn btn-md btn-primary btn-block' do
              계정 만들기
          %p.text-center
            이미 계정을 만드셨나요?
            = link_to '로그인', new_user_session_path, class: 'text-primary'
