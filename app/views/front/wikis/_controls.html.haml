- if can?(:update, current_wiki) && current_wiki.active?
  = link_to edit_front_post_path(current_wiki.post, list_nav: list_nav_params), class: 'btn btn-primary btn-sm', data: { 'turbolinks-action': 'replace' } do
    편집
